This is a simple prototype for the facial recognition software that we will be implementing on the raspberry Pi. 

This version runs on windows and uses whatever webcam you have connected. Final deployment will be done via docker onto the rasberry pi. Door lock/ unlock mechanism is simulated via code. 

To enroll new users, run enroll.py and follow instructions in the terminal. 

Run main.py to test the main secuirty system. 

